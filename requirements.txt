# ── Database ───────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9          # Postgres driver (binary = no libpq compile needed)
requests==2.31.0              # HTTP requests to Supabase REST API
 
# ── Spotify API (deprecated branch - kept as architectural reference) ───────────
# spotipy>=2.24               # Spotify Web API client - DO NOT USE for ML training
                               # Spotify 2025 ToS prohibits API data in ML models
 
# ── Environment / config ───────────────────────────────────────────────────────
python-dotenv>=1.0            # Loads .env into os.environ
pyyaml>=6.0                   # Reads config.yaml hyperparameters (DataCleaning etc.)
 
# ── Progress + logging ─────────────────────────────────────────────────────────
tqdm>=4.66                    # Progress bars in ingestion stages
logging (built-in)              # Standard logging for pipeline stages and errors
 
# ── Data ───────────────────────────────────────────────────────────────────────
pandas>=2.2                   # DataFrames throughout the pipeline
numpy>=1.26                   # Numerical ops on matrices/arrays
scikit-learn>=1.4             # Utilities and baseline models (KNN etc.)
scipy>=1.13                   # CSR sparse matrix construction (DataIngestion)
 
# ── Collaborative filtering model ──────────────────────────────────────────────
implicit>=0.7                 # ALS and BPR model training, model evaluation metrics
 
# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0                   # Unit tests for all pipeline stages
 
# ── Jupyter (optional - dev exploration) ───────────────────────────────────────
notebook>=7.0                 # Jupyter notebook
ipykernel>=6.29               # Kernel for VS Code notebook support